/*[IF-FLASH]*/
package tf
{



	/**
	 * moments
	 * @param x ( tf.Tensor | TypedArray |Array) The input tensor.
	 * @param axis (number|number[]) The dimension(s) along with to compute mean and
              variance. By default it reduces all dimensions.
	 * @param keepDims (boolean) If true, the moments have the same dimensionality as the
              input.
	 * @return {mean: tf.Tensor , variance: tf.Tensor }
	 */
	public function moments(x:*=null,axis:*=null,keepDims:*=null):*
	{
		return ;
	}


}