/*[IF-FLASH]*/
package tf
{

	import tf.Tensor;

	/**
	 * randomUniform
	 * @param shape (number[]) An array of integers defining the output tensor shape.
	 * @param minval (number) The lower bound on the range of random values to generate.
              Defaults to 0.
	 * @param maxval (number) The upper bound on the range of random values to generate.
              Defaults to 1.
	 * @param dtype ('float32'|'int32'|'bool') The data type of the output tensor. Defaults to 'float32'.
	 * @return tf.Tensor
	 */
	public function randomUniform(shape:*=null,minval:*=null,maxval:*=null,dtype:*=null):Tensor
	{
		return null;
	}


}