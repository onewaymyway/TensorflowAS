/*[IF-FLASH]*/
package tf
{



	/**
	 * reverse
	 * @param x ( tf.Tensor | TypedArray |Array) The input tensor to be reversed.
	 * @param axis (number|number[]) The set of dimensions to reverse. Must be in the
              range [-rank(x), rank(x)). Defaults to all axes.
	 * @return tf.Tensor
	 */
	public function reverse(x:*=null,axis:*=null):*
	{
		return ;
	}


}