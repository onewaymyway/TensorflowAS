/*[IF-FLASH]*/
package tf
{

	import tf.Tensor;

	/**
	 * logSumExp
	 * @param x ( tf.Tensor | TypedArray |Array) The input tensor.
	 * @param axis (number|number[]) The dimension(s) to reduce. If null (the default),
              reduces all dimensions.
	 * @param keepDims (boolean) If true, retains reduced dimensions with length
              of 1. Defaults to false.
	 * @return tf.Tensor
	 */
	public function logSumExp(x:*=null,axis:*=null,keepDims:*=null):Tensor
	{
		return null;
	}


}