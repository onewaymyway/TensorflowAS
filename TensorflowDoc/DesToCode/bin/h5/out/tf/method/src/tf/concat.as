/*[IF-FLASH]*/
package tf
{

	import tf.Tensor;

	/**
	 * concat
	 * @param tensors ( tf.Tensor []| TypedArray |Array[]) A list of tensors to concatenate.
	 * @param axis (number) The axis to concate along. Defaults to 0 (the first dim).
	 * @return tf.Tensor
	 */
	public function concat(tensors:*=null,axis:*=null):Tensor
	{
		return null;
	}


}