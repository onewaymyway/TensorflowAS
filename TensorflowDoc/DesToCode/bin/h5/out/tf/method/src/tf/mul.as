/*[IF-FLASH]*/
package tf
{

	import tf.Tensor;

	/**
	 * mul
	 * @param a ( tf.Tensor | TypedArray |Array) The first tensor to multiply.
	 * @param b ( tf.Tensor | TypedArray |Array) The second tensor to multiply. Must have the same dtype as a .
	 * @return tf.Tensor
	 */
	public function mul(a:*=null,b:*=null):Tensor
	{
		return null;
	}


}