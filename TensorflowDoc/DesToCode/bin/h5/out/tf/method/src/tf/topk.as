/*[IF-FLASH]*/
package tf
{



	/**
	 * topk
	 * @param x ( tf.Tensor | TypedArray |Array) 1-D or higher tf.Tensor with last dimension being at least k .
	 * @param k (number) Number of top elements to look for along the last dimension.
	 * @param sorted (boolean) If true, the resulting k elements will be sorted by the
              values in descending order.
	 * @return {values: tf.Tensor , indices: tf.Tensor }
	 */
	public function topk(x:*=null,k:*=null,sorted:*=null):*
	{
		return ;
	}


}