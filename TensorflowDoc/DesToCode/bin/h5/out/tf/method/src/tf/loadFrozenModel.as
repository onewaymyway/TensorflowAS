/*[IF-FLASH]*/
package tf
{

	import Promise;

	/**
	 * loadFrozenModel
	 * @param modelUrl (string) url for the model file generated by scripts/convert.py
              script.
	 * @param weightsManifestUrl (string) 
	 * @param requestOption (RequestInit) options for Request, which allows to send credentials
              and custom headers.
	 * @return Promise
	 */
	public function loadFrozenModel(modelUrl:*=null,weightsManifestUrl:*=null,requestOption:*=null):Promise
	{
		return null;
	}


}