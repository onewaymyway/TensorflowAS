/*[IF-FLASH]*/
package tf
{

	import tf.Tensor;

	/**
	 * stack
	 * @param tensors ( tf.Tensor []| TypedArray |Array[]) A list of tensor objects with the same shape and dtype.
	 * @param axis (number) The axis to stack along. Defaults to 0 (the first dim).
	 * @return tf.Tensor
	 */
	public function stack(tensors:*=null,axis:*=null):Tensor
	{
		return null;
	}


}