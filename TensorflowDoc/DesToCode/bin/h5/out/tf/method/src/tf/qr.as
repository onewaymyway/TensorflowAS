/*[IF-FLASH]*/
package tf
{



	/**
	 * qr
	 * @param x ( tf.Tensor ) The tf.Tensor to be QR-decomposed. Must have rank >= 2. Suppose
              it has the shape [..., M, N] .
	 * @param fullMatrices (boolean) An optional boolean parameter. Defaults to false .
              If true , compute full-sized Q . If false (the default),
              compute only the leading N columns of Q and R .
	 * @return [ tf.Tensor , tf.Tensor ]
	 */
	public function qr(x:*=null,fullMatrices:*=null):*
	{
		return ;
	}


}