/*[IF-FLASH]*/
package tf
{



	/**
	 * tidy
	 * @param nameOrFn (string|Function) The name of the closure, or the function to execute.
              If a name is provided, the 2nd argument should be the function.
              If debug mode is on, the timing and the memory usage of the function
              will be tracked and displayed on the console using the provided name.
	 * @param fn (Function) The function to execute.
	 * @param gradMode (boolean) 
	 * @return void|number|string| tf.Tensor | tf.Tensor []|{[key:
string]: tf.Tensor |number|string}
	 */
	public function tidy(nameOrFn:*=null,fn:*=null,gradMode:*=null):*
	{
		return ;
	}


}